spring:
  datasource:
    url: jdbc:mysql://localhost:3306/qa_notice_board
    username: root
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
logging:
  level:
    org.springframework.security: DEBUG
jwt:
  secret: qna_super_secret_key_that_is_definitely_long_enough_to_sign_hs512_safely_2025